function doubleA(arr) {
    //write your code here
    return arr.map((item) => item*2)
}
console.log(doubleA([1,2,3]))

const doubleA = a => a.map(n => n * 2);

  arr.forEach((item, index) => arr[index] += item);
    return arr;

    let newArr = [];
    arr.forEach((i) => newArr.push(i * 2));
    return newArr;
