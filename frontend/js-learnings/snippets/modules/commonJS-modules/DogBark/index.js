const Bark = require('./dogBark.js'); // or simple 'dogBark' .js is optional
console.log(Bark());

// Generally, it doesn't matter how you'll name the required module,
// but it might be a good idea to name it the same as the file or class name in it.